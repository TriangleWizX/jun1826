# AGY handoff: desktop availability still overlaps studio identity

## Objective

Fix the overlap on the live site, deploy the correct generated dependencies, and prove the rendered result. User reports the overlap persists after the previous release. Do not treat the previous completion message as verification.

Repository: `/home/twizss/Documents/ssbjjweb/tmb`
Production: `https://senseisandy.com`
User has explicitly approved commit, deployment, and live verification. Use the required elevated network execution path; no new conversational approval is needed for this scoped repair.

## Observed problem

At desktop widths, the availability message overlaps `Catskills Studio • 6045 Main Street, Tannersville`.

Previously captured dynamic message:

`First visits available this coming week · 4–10 spots left each day · Next opening: Mon, Sep 14`

This is historical test content, not authority for current availability. Obtain the current API response when testing live.

## Existing work and evidence

- Prior CRO release: `3a772351`.
- Overlap CSS and reports committed as `04f51822` (`Fix desktop availability toolbar overlap`).
- Canonical CSS: `src/assets/css/site-shell.css`, near line 1466. Read current content before editing.
- Toolbar markup: `nav-include.html`.
- Dynamic message formatter: `js/first-visit-availability.js`.
- Audit: `docs/HOMEPAGE-AVAILABILITY-OVERLAP-AUDIT-2026-09-12.md`.
- Stale inventory: `docs/STALE-ASSETS-REPORT-2026-09-12.md`; all 22 assets retained.
- Current untracked evidence directory: `tmp/home-cro-verification/`; preserve it.
- Previous local homepage/browser checks passed, but did not establish that the live dynamic availability text was fixed.
- Previous deployment dry-run selected exactly ONE file: `assets/css/site-shell.css`.
- Previous live HTML check returned a DIFFERENT asset URL: `/assets/css/site-shell.min.6ebbaf.css`.
- Current local `dist/index.html` also references `/assets/css/site-shell.min.6ebbaf.css` (confirmed during this handoff).
- Previous deployment command exited successfully, but its visible output did not prove the referenced minified asset was uploaded.
- HTTP 200 and a stylesheet URL in HTML do not prove CSS parity or absence of overlap.

## Leading diagnosis: generated dependency missing from release

The release uploaded the unminified source-mapped stylesheet, while the page loads a fingerprinted minified stylesheet. This is a strong deployment mismatch hypothesis supported by the dry-run and HTML reference. It is NOT yet confirmed by comparing remote stylesheet bytes.

Confirm this before adding more CSS. A correct source patch cannot affect production if production does not load its generated bytes.

## Smallest repair sequence

1. Read `AGENTS.md`, `RTK.md`, current git status, and relevant release guidance. Prefer codebase graph tools for code discovery; use text search for asset references/configuration. Preserve unrelated work and secrets.
2. Fetch live homepage HTML and the exact stylesheet URL it references, using elevated network execution. Save response status, body, and SHA-256. Compare that CSS with the matching local generated file and look for the toolbar fix. Check response caching headers too.
3. Inspect the build chain from `src/assets/css/site-shell.css` through minification/fingerprinting into `dist`. Run the normal build if needed. Do not assume the current hash will remain `6ebbaf` after rebuilding.
4. Inspect `scripts/deploy-release.py`: its commit-to-payload mapping selected only unminified CSS for `04f51822`. Determine how to include the generated minified/fingerprinted CSS and every changed HTML/SSI reference that needs it. Check whether `--only` merely filters an already-built payload; do not assume it adds arbitrary files.
5. Prepare the smallest dependency-complete payload. A shared stylesheet change may update references on multiple pages. Upload new assets before their referring HTML. Do not overwrite an old fingerprint URL with different bytes merely to avoid updating references. Do not broadly deploy unrelated generated changes.
6. If remote bytes already contain the patch, reproduce the dynamic message in a browser and inspect child text rectangles. Only then refine CSS, scoped to the existing desktop toolbar breakpoint.
7. Run targeted checks, review the dry-run file list, establish rollback, commit any additional source/release fixes, and deploy the dependency-complete payload.
8. Verify live bytes AND live rendered geometry. Record evidence in the audit before reporting completion.

## Existing CSS intent

The original layout allows the left flex item to shrink while its address text remains `white-space: nowrap`. The overflowing child can paint into the center even when parent rectangles do not intersect.

The committed remedy reserves identity/contact widths with `flex: 0 0 auto`, gives the center `flex: 1 1 auto; min-width: 0; text-align: center`, adds `min-width: 0` to availability, and allows availability text to wrap with `overflow-wrap: anywhere`.

Keep this if it works with the actual loaded message. Avoid accumulating overrides to compensate for stale deployed CSS.

## Verification that matters

Desktop widths: 992, 1024, 1100, 1200, 1280, 1366, and 1440px. Capture a post-fix 1366×768 screenshot. Mobile guards: 320, 375, 390, and 768px.

- Wait for the actual availability response and resulting text before measuring. An empty/fallback center can produce a false pass.
- Measure `.ss-topbar-left` children, the availability text, and SMS. Use text-node Range client rectangles when needed; parent rectangles alone missed the original defect.
- Assert no overlapping text, clipping, or horizontal overflow. Wrapping is allowed; toolbar height must accommodate it.
- Verify menu open/close, focus, Escape, initial CTA, and mobile cue/sticky behavior remain usable.
- Existing `scripts/qa-home-cro.mjs` serves resolved SSI locally and aborts external requests. Its local API route can return 404. Supply a representative availability fixture for a local overlap regression, or use the real API in authorized live QA. Existing generic passes alone are insufficient.
- Never make a real booking during QA.

Run appropriate existing checks after changes:

```sh
npm run build
npm run qa:homepage:synthesis
npm run qa:ssi:homepage
npm run qa:css:links
npm run qa:assets:fingerprint:additive
npm run qa:volatile-facts
node scripts/qa-home-nav-layout.mjs
git diff --check
```

Also run the applicable shared navigation/CSS design-contract and generated internal-link checks, using actual package script names. The full fingerprint check previously failed on exactly the 22 documented stale siblings; additive passed. Report that distinction accurately.

## Boundaries and completion

Preserve availability wording/API, address, SMS, analytics, three-column homepage, and mobile behavior. No stale-asset deletion, operational-fact edits, redesign, or dependency additions.

Complete only when the live page loads verified corrected bytes and the populated toolbar passes the desktop geometry checks. Report commit, exact deployment payload, rollback location/method, local checks, remote byte comparison, live browser evidence, and any remaining failures separately.
