# Architecture Docs

Purpose: explain how the site is organized so future updates are safe and fast.

## Include Here

- Rendering model (static HTML + shared partials/includes)
- Data sources (`assets/data/*.json`, scripts generating pages)
- Routing and URL conventions
- Build/QA pipeline notes

See also: `docs/architecture-high-level.md`
