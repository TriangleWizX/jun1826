# Archive Policy — SenseiSandy.com

## Archive Directives
1. All retired, obsolete, or consolidated source files MUST be preserved locally under `_archive/` and committed to Git before active removal.
2. `_archive/` is excluded from Eleventy builds (`eleventy.config.js`).
3. Archived files MUST NOT be output to `dist/` or indexed by search engines.
